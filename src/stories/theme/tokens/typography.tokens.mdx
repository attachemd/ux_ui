import { Meta, Unstyled } from "@storybook/addon-docs/blocks";

<Meta title="Theme/Tokens/Typography"/>

# Typography Tokens

The typography system defines font sizes, weights, letter-spacing (tracking), and line-height (leading) for consistent type rendering across the application.

<Unstyled>
  <style>{`
    .typo-section { margin-bottom: 48px; }
    .typo-section > h2 {
      font-size: 1rem;
      font-weight: 700;
      color: var(--ft-color-solid-on-surface);
      text-transform: uppercase;
      letter-spacing: 0.08em;
      margin: 0 0 16px 0;
      padding-bottom: 8px;
      border-bottom: 1px solid var(--ft-color-n-surface-15);
    }
    .size-row {
      display: flex;
      align-items: baseline;
      gap: 24px;
      padding: 12px 16px;
      border-radius: 10px;
      transition: background 0.2s;
      border-bottom: 1px solid var(--ft-color-n-on-surface-5);
    }
    .size-row:hover { background: var(--ft-color-n-surface-5); }
    .size-sample {
      flex: 1;
      color: var(--ft-color-solid-on-surface);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .size-meta {
      display: flex;
      gap: 16px;
      align-items: center;
      flex-shrink: 0;
    }
    .size-varname {
      font-family: 'Fira Code', monospace;
      font-size: 0.7rem;
      color: var(--ft-color-on-surface-muted);
    }
    .size-px {
      font-family: 'Fira Code', monospace;
      font-size: 0.75rem;
      font-weight: 700;
      color: var(--ft-color-primary-500);
      min-width: 36px;
      text-align: right;
    }
    .weight-row {
      display: flex;
      align-items: center;
      gap: 24px;
      padding: 10px 16px;
      border-radius: 10px;
      border-bottom: 1px solid var(--ft-color-n-on-surface-5);
      transition: background 0.2s;
    }
    .weight-row:hover { background: var(--ft-color-n-surface-5); }
    .weight-sample {
      font-size: 1.25rem;
      color: var(--ft-color-solid-on-surface);
      flex: 1;
    }
    .scale-row {
      display: flex;
      align-items: center;
      gap: 16px;
      padding: 10px 16px;
      border-radius: 10px;
      border-bottom: 1px solid var(--ft-color-n-on-surface-5);
      transition: background 0.2s;
    }
    .scale-row:hover { background: var(--ft-color-n-surface-5); }
    .scale-label {
      font-family: 'Fira Code', monospace;
      font-size: 0.8rem;
      color: var(--ft-color-on-surface-muted);
      min-width: 200px;
      flex-shrink: 0;
    }
    .scale-value {
      font-family: 'Fira Code', monospace;
      font-size: 0.8rem;
      font-weight: 700;
      color: var(--ft-color-solid-on-surface);
      min-width: 80px;
      flex-shrink: 0;
    }
    .tracking-demo {
      font-size: 0.9rem;
      color: var(--ft-color-on-surface-muted);
    }
  `}</style>
</Unstyled>

export const ftTextSizes = [
    { name: '--ft-text-4xs', px: '10px' },
    { name: '--ft-text-3xs', px: '11px' },
    { name: '--ft-text-2xs', px: '12px' },
    { name: '--ft-text-xs', px: '13px' },
];

export const textSizes = [
    { name: '--text-xs', rem: '0.75rem', px: '12px' },
    { name: '--text-sm', rem: '0.875rem', px: '14px' },
    { name: '--text-base', rem: '1rem', px: '16px' },
    { name: '--text-lg', rem: '1.125rem', px: '18px' },
    { name: '--text-xl', rem: '1.25rem', px: '20px' },
    { name: '--text-2xl', rem: '1.5rem', px: '24px' },
    { name: '--text-3xl', rem: '1.875rem', px: '30px' },
    { name: '--text-4xl', rem: '2.25rem', px: '36px' },
    { name: '--text-5xl', rem: '3rem', px: '48px' },
];

export const fontWeights = [
    { name: '--font-weight-thin', value: 100 },
    { name: '--font-weight-extralight', value: 200 },
    { name: '--font-weight-light', value: 300 },
    { name: '--font-weight-normal', value: 400 },
    { name: '--font-weight-medium', value: 500 },
    { name: '--font-weight-semibold', value: 600 },
    { name: '--font-weight-bold', value: 700 },
    { name: '--font-weight-extrabold', value: 800 },
    { name: '--font-weight-black', value: 900 },
];

export const tracking = [
    { name: '--tracking-tighter', value: '-0.05em', label: 'Condensed headlines' },
    { name: '--tracking-tight', value: '-0.025em', label: 'Compact UI text' },
    { name: '--tracking-normal', value: '0em', label: 'Body text default' },
    { name: '--tracking-wide', value: '0.025em', label: 'Subtle expansion' },
    { name: '--tracking-wider', value: '0.05em', label: 'Display text' },
    { name: '--tracking-widest', value: '0.1em', label: 'Artistic headings' },
];

<Unstyled>
  <div class="typo-section">
    <h2>Font Sizes — FT Scale</h2>
    {ftTextSizes.map(t => (
      <div class="size-row" key={t.name}>
        <span class="size-sample" style={{ fontSize: t.px }}>The quick brown fox jumps over the lazy dog</span>
        <div class="size-meta">
          <code class="size-varname">{t.name}</code>
          <span class="size-px">{t.px}</span>
        </div>
      </div>
    ))}
  </div>

  <div class="typo-section">
    <h2>Font Sizes — Base Scale</h2>
    {textSizes.map(t => (
      <div class="size-row" key={t.name}>
        <span class="size-sample" style={{ fontSize: t.rem }}>The quick brown fox</span>
        <div class="size-meta">
          <code class="size-varname">{t.name}</code>
          <span class="size-px">{t.px}</span>
        </div>
      </div>
    ))}
  </div>

  <div class="typo-section">
    <h2>Font Weights</h2>
    {fontWeights.map(t => (
      <div class="weight-row" key={t.name}>
        <span class="weight-sample" style={{ fontWeight: t.value }}>
          {t.value} — The quick brown fox
        </span>
        <code class="size-varname">{t.name}</code>
      </div>
    ))}
  </div>

  <div class="typo-section">
    <h2>Letter Spacing (Tracking)</h2>
    {tracking.map(t => (
      <div class="scale-row" key={t.name}>
        <code class="scale-label">{t.name}</code>
        <span class="scale-value">{t.value}</span>
        <span class="tracking-demo" style={{ letterSpacing: t.value }}>SAMPLE TEXT • {t.label}</span>
      </div>
    ))}
  </div>
</Unstyled>
