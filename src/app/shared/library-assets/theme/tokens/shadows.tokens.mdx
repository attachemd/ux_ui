import { Meta, Unstyled } from "@storybook/addon-docs/blocks";

<Meta title="Theme/Tokens/Shadows"/>

# Shadow Tokens

Shadow tokens establish the elevation system. Use box shadows to create depth, drop shadows for float effects, and text shadows for special typographic treatments.

<Unstyled>
  <style>{`
    .shadow-section { margin-bottom: 40px; }
    .shadow-section h2 {
      font-size: 1.1rem;
      font-weight: 700;
      color: var(--ft-color-solid-on-surface);
      margin: 0 0 16px 0;
      padding-bottom: 8px;
      border-bottom: 1px solid var(--ft-color-n-surface-15);
    }
    .shadow-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
      gap: 24px;
      padding: 8px 0 16px;
    }
    .shadow-card {
      background: var(--ft-color-solid-surface);
      border: 1px solid var(--ft-color-n-surface-10);
      border-radius: 14px;
      padding: 24px 16px 16px;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 16px;
      cursor: default;
    }
    .shadow-box {
      width: 64px;
      height: 64px;
      background: var(--ft-color-solid-surface);
      border-radius: 10px;
      border: 1px solid var(--ft-color-n-surface-10);
    }
    .shadow-text-sample {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--ft-color-solid-on-surface);
    }
    .shadow-meta { text-align: center; }
    .shadow-varname {
      font-family: 'Fira Code', monospace;
      font-size: 0.72rem;
      color: var(--ft-color-on-surface-muted);
      display: block;
    }
  `}</style>
</Unstyled>

export const boxShadowTokens = [
    { name: '--shadow-2xs', value: '0 1px rgb(0 0 0 / 0.05)' },
    { name: '--shadow-xs', value: '0 1px 2px 0 rgb(0 0 0 / 0.05)' },
    { name: '--shadow-sm', value: '0 1px 3px 0 rgb(0 0 0 / 0.1)' },
    { name: '--shadow-md', value: '0 4px 6px -1px rgb(0 0 0 / 0.1)' },
    { name: '--shadow-lg', value: '0 10px 15px -3px rgb(0 0 0 / 0.1)' },
    { name: '--shadow-xl', value: '0 20px 25px -5px rgb(0 0 0 / 0.1)' },
    { name: '--shadow-2xl', value: '0 25px 50px -12px rgb(0 0 0 / 0.25)' },
];

export const dropShadowTokens = [
    { name: '--drop-shadow-xs', value: '0 1px 1px rgb(0 0 0 / 0.05)' },
    { name: '--drop-shadow-sm', value: '0 1px 2px rgb(0 0 0 / 0.15)' },
    { name: '--drop-shadow-md', value: '0 3px 3px rgb(0 0 0 / 0.12)' },
    { name: '--drop-shadow-lg', value: '0 4px 4px rgb(0 0 0 / 0.15)' },
    { name: '--drop-shadow-xl', value: '0 9px 7px rgb(0 0 0 / 0.1)' },
    { name: '--drop-shadow-2xl', value: '0 25px 25px rgb(0 0 0 / 0.15)' },
];

export const textShadowTokens = [
    { name: '--text-shadow-2xs', value: '0px 1px 0px rgb(0 0 0 / 0.15)' },
    { name: '--text-shadow-xs', value: '0px 1px 1px rgb(0 0 0 / 0.2)' },
    { name: '--text-shadow-sm', value: '0px 1px 0px rgb(0 0 0 / 0.075)' },
    { name: '--text-shadow-md', value: '0px 1px 1px rgb(0 0 0 / 0.1)' },
    { name: '--text-shadow-lg', value: '0px 1px 2px rgb(0 0 0 / 0.1)' },
];

export const ShadowCard = ({ name, value }) => (
    <div class="shadow-card">
        <div class="shadow-box" style={{ boxShadow: value }}></div>
        <div class="shadow-meta">
            <code class="shadow-varname">{name}</code>
        </div>
    </div>
);

export const TextShadowCard = ({ name, value }) => (
    <div class="shadow-card">
        <div class="shadow-text-sample" style={{ textShadow: value }}>Aa</div>
        <div class="shadow-meta">
            <code class="shadow-varname">{name}</code>
        </div>
    </div>
);

<Unstyled>
  <div class="shadow-section">
    <h2>Box Shadows</h2>
    <div class="shadow-grid">
      {boxShadowTokens.map(t => <ShadowCard key={t.name} {...t}/>)}
    </div>
  </div>

  <div class="shadow-section">
    <h2>Drop Shadows</h2>
    <div class="shadow-grid">
      {dropShadowTokens.map(t => <ShadowCard key={t.name} {...t}/>)}
    </div>
  </div>

  <div class="shadow-section">
    <h2>Text Shadows</h2>
    <div class="shadow-grid">
      {textShadowTokens.map(t => <TextShadowCard key={t.name} {...t}/>)}
    </div>
  </div>
</Unstyled>
