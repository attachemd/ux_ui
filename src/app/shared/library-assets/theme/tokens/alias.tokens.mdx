import { Meta, Unstyled } from "@storybook/addon-docs/blocks";

<Meta title="Theme/Tokens/Alias Tokens"/>

# Alias Tokens

Alias tokens bridge the raw palette and the design system's semantic layer. Each token maps to a specific source token, making it easy to swap themes or rebrand without touching component code.

<Unstyled>
  <style>{`
    .alias-page { padding: 24px 0; display: flex; flex-direction: column; gap: 48px; }

    .alias-section-title {
      font-size: 1rem; font-weight: 700; text-transform: uppercase;
      letter-spacing: 0.08em; color: var(--ft-color-solid-on-surface);
      padding-bottom: 10px; border-bottom: 2px solid var(--ft-color-n-surface-15);
      margin: 0 0 16px 0;
    }
    .alias-section-desc {
      font-size: 0.8rem; color: var(--ft-color-on-surface-muted);
      margin: -8px 0 16px 0;
    }

    /* Palette card for color scale aliases */
    .palette-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 16px;
    }
    .palette-card {
      background: var(--ft-color-solid-surface);
      border: 1px solid var(--ft-color-n-surface-15);
      border-radius: 16px; overflow: hidden;
      box-shadow: 0 2px 6px rgba(0,0,0,0.06);
      transition: all 0.25s;
    }
    .palette-card:hover { transform: translateY(-3px); box-shadow: 0 8px 18px rgba(0,0,0,0.1); }
    .palette-header {
      padding: 10px 14px;
      border-bottom: 1px solid var(--ft-color-n-surface-10);
      background: var(--ft-color-n-surface-5);
      display: flex; align-items: center; justify-content: space-between;
    }
    .palette-title { font-size: 1rem; font-weight: 700; margin: 0; color: var(--ft-color-solid-on-surface); }
    .palette-source { font-family: 'Fira Code', monospace; font-size: 0.65rem; color: var(--ft-color-on-surface-muted); }
    .palette-shades { display: flex; height: 40px; }
    .palette-shade { flex: 1; }
    .palette-rows { padding: 6px 10px; display: flex; flex-direction: column; gap: 1px; }
    .shade-row {
      display: flex; align-items: center; gap: 8px;
      padding: 4px 6px; border-radius: 5px; transition: background 0.15s;
    }
    .shade-row:hover { background: var(--ft-color-n-surface-8); }
    .shade-swatch { width: 36px; height: 18px; border-radius: 3px; box-shadow: inset 0 0 0 1px rgba(0,0,0,0.06); flex-shrink: 0; }
    .shade-key { font-family: 'Fira Code', monospace; font-size: 0.68rem; font-weight: 600; min-width: 28px; color: var(--ft-color-solid-on-surface); }
    .shade-name { font-family: 'Fira Code', monospace; font-size: 0.65rem; color: var(--ft-color-on-surface-muted); flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
    .shade-arrow { font-size: 0.6rem; color: var(--ft-color-on-surface-muted); flex-shrink: 0; }
    .shade-source { font-family: 'Fira Code', monospace; font-size: 0.6rem; color: var(--ft-color-on-surface-muted); flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; text-align: right; }

    /* ---- Surface Section ---- */
    .surface-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
      gap: 16px;
    }
    .surface-card {
      border-radius: 14px; overflow: hidden;
      border: 1px solid var(--ft-color-n-surface-15);
      box-shadow: 0 2px 6px rgba(0,0,0,0.06);
      transition: all 0.25s;
    }
    .surface-card:hover { transform: translateY(-2px); box-shadow: 0 8px 16px rgba(0,0,0,0.1); }
    .surface-preview {
      padding: 14px;
      display: flex; align-items: center; gap: 10px;
    }
    .surface-text-demo { flex: 1; }
    .surface-text-demo span { display: block; font-weight: 600; font-size: 0.85rem; }
    .surface-text-demo small { font-size: 0.7rem; }
    .surface-icon { width: 36px; height: 36px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 1rem; flex-shrink: 0; }
    .surface-meta {
      padding: 10px 14px;
      background: var(--ft-color-solid-surface);
      border-top: 1px solid var(--ft-color-n-surface-10);
    }
    .surface-varname { font-family: 'Fira Code', monospace; font-size: 0.75rem; color: var(--ft-color-solid-on-surface); display: block; }
    .surface-source  { font-family: 'Fira Code', monospace; font-size: 0.65rem; color: var(--ft-color-on-surface-muted); display: block; margin-top: 2px; }
    .surface-usage   { font-size: 0.7rem; color: var(--ft-color-primary-500); font-weight: 600; margin-top: 4px; display: block; }

    /* ---- Surface Text Hierarchy Demo ---- */
    .text-hierarchy {
      background: var(--ft-color-solid-surface);
      border: 1px solid var(--ft-color-n-surface-15);
      border-radius: 14px; padding: 20px; display: flex; flex-direction: column; gap: 12px;
    }
    .text-level {
      display: flex; align-items: center; gap: 14px;
    }
    .text-swatch { width: 32px; height: 32px; border-radius: 6px; box-shadow: inset 0 0 0 1px rgba(0,0,0,0.06); flex-shrink: 0; }
    .text-label { font-size: 0.85rem; font-weight: 600; flex: 1; }
    .text-meta { font-family: 'Fira Code', monospace; font-size: 0.65rem; color: var(--ft-color-on-surface-muted); text-align: right; }

    /* ---- Border / Shadow Section ---- */
    .border-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 16px;
    }
    .border-card {
      background: var(--ft-color-solid-surface);
      border-radius: 14px; overflow: hidden;
      border: 1px solid var(--ft-color-n-surface-10);
      transition: all 0.25s;
    }
    .border-card:hover { transform: translateY(-2px); box-shadow: 0 8px 16px rgba(0,0,0,0.08); }
    .border-preview {
      padding: 20px; display: flex; align-items: center; justify-content: center;
      background: var(--ft-color-n-surface-5);
    }
    .border-example-box {
      width: 80px; height: 50px; border-radius: 8px;
      background: var(--ft-color-solid-surface);
      display: flex; align-items: center; justify-content: center;
      font-size: 0.65rem; font-family: 'Fira Code', monospace; color: var(--ft-color-on-surface-muted);
    }
    .shadow-example-box {
      width: 80px; height: 50px; border-radius: 8px;
      background: var(--ft-color-solid-surface);
    }
    .border-meta { padding: 12px 14px; }
    .border-varname { font-family: 'Fira Code', monospace; font-size: 0.75rem; color: var(--ft-color-solid-on-surface); display: block; }
    .border-source  { font-family: 'Fira Code', monospace; font-size: 0.65rem; color: var(--ft-color-on-surface-muted); display: block; margin-top: 2px; }
    .border-usage   { font-size: 0.7rem; color: var(--ft-color-primary-500); font-weight: 600; margin-top: 6px; display: block; }

    /* Pair rows for role + on-role */
    .role-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 16px;
    }
    .role-pair {
      border-radius: 12px; overflow: hidden;
      border: 1px solid var(--ft-color-n-surface-15);
      box-shadow: 0 2px 6px rgba(0,0,0,0.05);
    }
    .role-main { height: 56px; display: flex; align-items: center; justify-content: center; }
    .role-on { height: 28px; display: flex; align-items: center; padding: 0 14px; gap: 8px;
      border-top: 1px solid rgba(0,0,0,0.06); }
    .role-varname { font-family: 'Fira Code', monospace; font-size: 0.7rem; }
    .role-label { font-family: 'Fira Code', monospace; font-size: 0.72rem; font-weight: 700; }
  `}</style>
</Unstyled>

export const scales = ['50', '100', '200', '300', '400', '500', '600', '700', '800', '900', '950'];

export const colorScaleAliases = [
  { title: 'Primary', prefix: '--ft-color-primary', source: '--ft-color-mauve-*' },
  { title: 'Secondary', prefix: '--ft-color-secondary', source: '--color-pink-*' },
  { title: 'Tertiary', prefix: '--ft-color-tertiary', source: '--color-neutral-*' },
  { title: 'Success', prefix: '--ft-color-success', source: '--color-green-*' },
  { title: 'Warning', prefix: '--ft-color-warning', source: '--color-orange-*' },
  { title: 'Danger', prefix: '--ft-color-danger', source: '--color-red-*' },
];

export const surfaceAliases = [
  { name: '--ft-color-surface-level-100', source: 'n-surface-85', bgToken: 'var(--ft-color-surface-level-100)', onToken: 'var(--ft-color-on-surface-level-100)', usage: 'Main page/app background', icon: '🗂️' },
  { name: '--ft-color-surface-level-200', source: 'n-on-surface-4', bgToken: 'var(--ft-color-surface-level-200)', onToken: 'var(--ft-color-on-surface-level-100)', usage: 'Cards, panels on the page', icon: '🃏' },
  { name: '--ft-color-surface-level-300', source: 'n-surface-100', bgToken: 'var(--ft-color-surface-level-300)', onToken: 'var(--ft-color-on-surface-level-100)', usage: 'Modals, overlays on cards', icon: '⬜' },
  { name: '--ft-color-surface-level-400', source: 'n-on-surface-6', bgToken: 'var(--ft-color-surface-level-400)', onToken: 'var(--ft-color-on-surface-level-100)', usage: 'Tooltips, popovers', icon: '💬' },
  { name: '--ft-color-surface-muted', source: 'n-on-surface-40', bgToken: 'var(--ft-color-surface-muted)', onToken: 'var(--ft-color-solid-surface)', usage: 'Disabled backgrounds', icon: '🔇' },
  { name: '--ft-color-surface-scroll', source: 'n-on-surface-0', bgToken: 'var(--ft-color-surface-scroll)', onToken: 'var(--ft-color-solid-surface)', usage: 'Scrollbar track', icon: '📜' },
];

export const onSurfaceLevels = [
  { name: '--ft-color-on-surface-level-100', source: 'n-on-surface-100', usage: 'Primary text, headings' },
  { name: '--ft-color-on-surface-level-200', source: 'n-on-surface-80', usage: 'Secondary text, labels' },
  { name: '--ft-color-on-surface-level-300', source: 'n-on-surface-40', usage: 'Tertiary text, captions' },
  { name: '--ft-color-on-surface-muted', source: 'n-on-surface-30', usage: 'Placeholder text' },
  { name: '--ft-color-on-surface-scroll', source: 'n-on-surface-15', usage: 'Scrollbar thumb' },
];

export const borderAliases = [
  { name: '--ft-color-border-100', source: 'n-on-surface-3', type: 'border', usage: 'Subtle dividers between list items or table rows' },
  { name: '--ft-color-border-200', source: 'n-on-surface-10', type: 'border', usage: 'Card and panel borders (default)' },
  { name: '--ft-color-border-300', source: 'n-on-surface-40', type: 'border', usage: 'Strong focus rings, active borders' },
  { name: '--ft-color-border-muted', source: 'n-on-surface-7', type: 'border', usage: 'Light container separators' },
  { name: '--ft-color-border-solid', source: 'n-on-surface-100', type: 'border', usage: 'High-contrast borders, inputs on focus' },
  { name: '--ft-color-shadow-200', source: 'n-on-surface-3', type: 'shadow', usage: 'Subtle card shadow overlay' },
  { name: '--ft-color-shadow-300', source: 'n-on-surface-5', type: 'shadow', usage: 'Modal / elevated panel shadow' },
];

export const roleColors = [
  { role: 'primary', bg: 'var(--ft-color-primary)', on: 'var(--ft-color-on-primary)', bgVar: '--ft-color-primary', onVar: '--ft-color-on-primary' },
  { role: 'secondary', bg: 'var(--ft-color-secondary)', on: 'var(--ft-color-on-secondary)', bgVar: '--ft-color-secondary', onVar: '--ft-color-on-secondary' },
  { role: 'success', bg: 'var(--ft-color-success)', on: 'var(--ft-color-on-success)', bgVar: '--ft-color-success', onVar: '--ft-color-on-success' },
  { role: 'warning', bg: 'var(--ft-color-warning)', on: 'var(--ft-color-on-warning)', bgVar: '--ft-color-warning', onVar: '--ft-color-on-warning' },
  { role: 'danger', bg: 'var(--ft-color-danger)', on: 'var(--ft-color-on-danger)', bgVar: '--ft-color-danger', onVar: '--ft-color-on-danger' },
  { role: 'info', bg: 'var(--ft-color-info)', on: 'var(--ft-color-on-info)', bgVar: '--ft-color-info', onVar: '--ft-color-on-info' },
];

export const PaletteAliasCard = ({ title, prefix, source }) => (
  <div className="palette-card">
    <div className="palette-header">
      <h3 className="palette-title">{title}</h3>
      <code className="palette-source">→ {source}</code>
    </div>
    <div className="palette-shades">
      {scales.map(s => (
        <div className="palette-shade" key={s} style={{ backgroundColor: `var(${prefix}-${s})` }}></div>
      ))}
    </div>
    <div className="palette-rows">
      {scales.map(s => (
        <div className="shade-row" key={s}>
          <div className="shade-swatch" style={{ backgroundColor: `var(${prefix}-${s})` }}></div>
          <span className="shade-key">{s}</span>
          <code className="shade-name">{prefix}-{s}</code>
        </div>
      ))}
    </div>
  </div>
);

export const SurfaceCard = ({ name, source, bgToken, onToken, usage, icon }) => (
  <div className="surface-card">
    <div className="surface-preview" style={{ backgroundColor: bgToken }}>
      <div className="surface-icon" style={{ background: onToken, color: bgToken }}>{icon}</div>
      <div className="surface-text-demo">
        <span style={{ color: onToken }}>Sample Text</span>
        <small style={{ color: onToken, opacity: 0.6 }}>Supporting description</small>
      </div>
    </div>
    <div className="surface-meta">
      <code className="surface-varname">{name}</code>
      <code className="surface-source">→ --ft-color-{source}</code>
      <span className="surface-usage">✦ {usage}</span>
    </div>
  </div>
);

export const TextHierarchyRow = ({ name, source, usage }) => (
  <div className="text-level">
    <div className="text-swatch" style={{ backgroundColor: `var(${name})` }}></div>
    <div className="text-label" style={{ color: `var(${name})` }}>{usage}</div>
    <div className="text-meta">{name}</div>
  </div>
);

export const BorderCard = ({ name, source, type, usage }) => (
  <div className="border-card">
    <div className="border-preview">
      {type === 'border'
        ? <div className="border-example-box" style={{ border: `1px solid var(${name})` }}>border</div>
        : <div className="shadow-example-box" style={{ boxShadow: `0 0 0 4px var(${name})` }}></div>
      }
    </div>
    <div className="border-meta">
      <code className="border-varname">{name}</code>
      <code className="border-source">→ --ft-color-{source}</code>
      <span className="border-usage">✦ {usage}</span>
    </div>
  </div>
);

export const RolePair = ({ role, bg, on, bgVar, onVar }) => (
  <div className="role-pair">
    <div className="role-main" style={{ backgroundColor: bg }}>
      <span className="role-label" style={{ color: on }}>{role.charAt(0).toUpperCase() + role.slice(1)}</span>
    </div>
    <div className="role-on" style={{ backgroundColor: on }}>
      <span className="role-varname" style={{ color: bg, flex: 1 }}>{onVar}</span>
    </div>
  </div>
);

<Unstyled>
  <div className="alias-page">

    <div>
      <div className="alias-section-title">Color Scale Aliases</div>
      <p className="alias-section-desc">Each semantic palette (Primary, Secondary…) is aliased from a raw color family.</p>
      <div className="palette-grid">
        {colorScaleAliases.map(p => <PaletteAliasCard key={p.title} {...p}/>)}
      </div>
    </div>

    <div>
      <div className="alias-section-title">Role Colors (with On-Colors)</div>
      <p className="alias-section-desc">Each role color is paired with a foreground "on" color for accessible contrast.</p>
      <div className="role-grid">
        {roleColors.map(r => <RolePair key={r.role} {...r}/>)}
      </div>
    </div>

    <div>
      <div className="alias-section-title">Surface Aliases</div>
      <p className="alias-section-desc">
        Surface tokens define background colors for UI layers — from the page background to modals and tooltips.
        Each card below previews the actual background with sample text rendered using the matching on-surface color.
      </p>
      <div className="surface-grid">
        {surfaceAliases.map(t => <SurfaceCard key={t.name} {...t}/>)}
      </div>
    </div>

    <div>
      <div className="alias-section-title">On-Surface (Text) Aliases</div>
      <p className="alias-section-desc">
        On-surface tokens control text and icon opacity. The preview below shows how each level looks rendered on the default surface.
      </p>
      <div className="text-hierarchy">
        {onSurfaceLevels.map(t => <TextHierarchyRow key={t.name} {...t}/>)}
      </div>
    </div>

    <div>
      <div className="alias-section-title">Border & Shadow Aliases</div>
      <p className="alias-section-desc">
        Each card shows the token applied live — border tokens display as the actual border around a box,
        while shadow tokens display as a glowing ring (box-shadow). A usage note explains where each belongs in the UI.
      </p>
      <div className="border-grid">
        {borderAliases.map(t => <BorderCard key={t.name} {...t}/>)}
      </div>
    </div>

  </div>
</Unstyled>
