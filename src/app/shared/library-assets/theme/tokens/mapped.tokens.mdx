import { Meta, Unstyled } from "@storybook/addon-docs/blocks";

<Meta title="Theme/Tokens/Mapped Tokens"/>

# Mapped Tokens

Mapped tokens are component-specific aliases that reference raw design tokens. They provide a consistent, semantic layer for shared UI patterns like form fields, switches, dialogs, and icons.

<Unstyled>
  <style>{`
    .mapped-page { padding: 24px 0; display: flex; flex-direction: column; gap: 48px; }

    .mapped-section-title {
      font-size: 1rem; font-weight: 700; text-transform: uppercase;
      letter-spacing: 0.08em; color: var(--ft-color-solid-on-surface);
      padding-bottom: 10px; border-bottom: 2px solid var(--ft-color-n-surface-15);
      margin: 0 0 8px 0;
    }
    .mapped-section-desc {
      font-size: 0.8rem; color: var(--ft-color-on-surface-muted);
      margin: 0 0 16px 0; line-height: 1.6;
    }

    /* ---- Token rows ---- */
    .mapped-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 10px;
    }
    .mapped-row {
      display: flex; align-items: center; gap: 12px;
      padding: 10px 14px; border-radius: 10px;
      border: 1px solid var(--ft-color-n-surface-10);
      background: var(--ft-color-solid-surface);
      transition: all 0.2s;
    }
    .mapped-row:hover { border-color: var(--ft-color-primary-300); background: var(--ft-color-n-surface-5); }
    .mapped-swatch { width: 32px; height: 32px; border-radius: 6px; flex-shrink: 0; box-shadow: inset 0 0 0 1px rgba(0,0,0,0.07); }
    .mapped-no-swatch {
      width: 32px; height: 32px; border-radius: 6px; flex-shrink: 0;
      background: repeating-linear-gradient(45deg, var(--ft-color-n-on-surface-5) 0px, var(--ft-color-n-on-surface-5) 4px, transparent 4px, transparent 8px);
      border: 1px solid var(--ft-color-n-on-surface-10);
      display: flex; align-items: center; justify-content: center;
      font-size: 0.9rem; color: var(--ft-color-on-surface-muted);
    }
    .mapped-info { flex: 1; min-width: 0; }
    .mapped-varname { font-family: 'Fira Code', monospace; font-size: 0.75rem; color: var(--ft-color-solid-on-surface); display: block; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
    .mapped-value   { font-family: 'Fira Code', monospace; font-size: 0.65rem; color: var(--ft-color-on-surface-muted); display: block; margin-top: 2px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
    .mapped-usage   { font-size: 0.68rem; color: var(--ft-color-primary-500); font-weight: 600; display: block; margin-top: 3px; }
    .mapped-badge { font-size: 0.6rem; padding: 2px 6px; border-radius: 4px; background: var(--ft-color-n-on-surface-8); color: var(--ft-color-on-surface-muted); text-transform: uppercase; font-weight: 700; flex-shrink: 0; }

    /* ---- Form Field ---- */
    .form-demo {
      background: var(--ft-color-solid-surface);
      border: 1px solid var(--ft-color-n-surface-15);
      border-radius: 14px; padding: 20px; margin-bottom: 16px;
      display: flex; flex-direction: column; gap: 10px;
    }
    .form-demo-label { font-size: 0.78rem; font-weight: 600; color: var(--ft-color-on-surface-muted); }
    .form-demo-input {
      border-radius: 8px; border: 1px solid var(--ft-color-n-on-surface-15);
      padding: 8px 12px; font-size: 0.85rem; outline: none;
      background: var(--ft-form-field-50); color: var(--ft-color-solid-on-surface);
    }
    .form-demo-input::placeholder { color: var(--ft-form-field-content-muted); }
    .form-demo-states { display: flex; gap: 10px; flex-wrap: wrap; }
    .form-demo-chip { padding: 4px 12px; border-radius: 99px; font-size: 0.72rem; font-weight: 600; }

    /* ---- Switch ---- */
    .switch-demo {
      background: var(--ft-color-solid-surface);
      border: 1px solid var(--ft-color-n-surface-15);
      border-radius: 14px; padding: 20px; margin-bottom: 16px;
      display: flex; flex-wrap: wrap; gap: 20px; align-items: center;
    }
    .switch-item { display: flex; flex-direction: column; align-items: center; gap: 8px; }
    .switch-track { width: 44px; height: 24px; border-radius: 99px; position: relative; }
    .switch-thumb { width: 18px; height: 18px; border-radius: 50%; position: absolute; top: 3px; }
    .switch-label { font-size: 0.7rem; font-weight: 600; color: var(--ft-color-on-surface-muted); text-align: center; }

    /* ---- Dialog Annotation ---- */
    .dialog-annotation-wrap { margin-bottom: 16px; }
    .dialog-annotation-box {
      position: relative;
      border: 2px solid var(--ft-color-n-on-surface-15);
      border-radius: 14px; overflow: hidden;
      background: var(--ft-color-solid-surface);
      min-height: 140px;
    }
    .ann-band { position: absolute; z-index: 1; display: flex; align-items: center; justify-content: center; }
    .ann-band.ann-v { left: 0; right: 0; height: 16px; background: rgba(139, 92, 246, 0.08); }
    .ann-band.ann-h { top: 0; bottom: 0; width: 32px; background: rgba(236, 72, 153, 0.08); }
    .ann-label {
      font-family: 'Fira Code', monospace; font-size: 0.58rem; font-weight: 700;
      white-space: nowrap; color: #7c3aed;
      background: rgba(237, 233, 254, 0.9);
      padding: 1px 5px; border-radius: 3px;
    }
    .ann-label-h {
      color: #be185d;
      background: rgba(252, 231, 243, 0.9);
      writing-mode: vertical-rl; text-orientation: mixed;
    }
    .ann-content { margin: 16px 32px; position: relative; z-index: 2; }
    .ann-title { font-weight: 700; font-size: 0.9rem; margin-bottom: 6px; color: var(--ft-color-solid-on-surface); }
    .ann-body  { font-size: 0.8rem; color: var(--ft-color-on-surface-muted); }
    .ann-legend { display: flex; gap: 12px; margin-top: 10px; flex-wrap: wrap; }
    .ann-legend-item {
      font-size: 0.72rem; font-weight: 600; padding: 4px 10px;
      border-radius: 6px; border: 2px dashed; color: var(--ft-color-solid-on-surface);
    }

    /* ---- Icon ---- */
    .ft-icon-demo {
      background: var(--ft-color-solid-surface);
      border: 1px solid var(--ft-color-n-surface-15);
      border-radius: 14px; padding: 20px; margin-bottom: 16px;
      display: flex; gap: 24px; align-items: center; flex-wrap: wrap;
    }
    .ft-icon-item { display: flex; flex-direction: column; align-items: center; gap: 8px; }
    .ft-icon-circle {
      width: 52px; height: 52px; min-width: 52px; min-height: 52px; border-radius: 50%;
      display: flex; align-items: center; justify-content: center;
      background: var(--ft-color-n-surface-10);
    }
    .ft-icon-svg {
      width: 24px; height: 24px; min-width: 24px; min-height: 24px;
      display: block; flex-shrink: 0;
    }
    .variant-svg {
      width: 22px; height: 22px; min-width: 22px; min-height: 22px;
      display: block; flex-shrink: 0;
    }
    .ft-icon-variant-list { padding: 8px 16px; border-left: 2px solid var(--ft-color-n-on-surface-10); flex: 1; min-width: 200px; }
    .ft-icon-label { font-size: 0.65rem; font-family: 'Fira Code', monospace; color: var(--ft-color-on-surface-muted); text-align: center; }

    /* ---- Radio ---- */
    .radio-demo {
      background: var(--ft-color-solid-surface);
      border: 1px solid var(--ft-color-n-surface-15);
      border-radius: 14px; padding: 20px; margin-bottom: 16px;
      display: flex; gap: 24px; flex-wrap: wrap; align-items: center;
    }
    .radio-item { display: flex; align-items: center; gap: 10px; font-size: 0.85rem; }
    .radio-circle {
      width: 20px; height: 20px; border-radius: 50%; border: 2px solid;
      display: flex; align-items: center; justify-content: center; flex-shrink: 0;
    }
    .radio-dot { width: 8px; height: 8px; border-radius: 50%; }
  `}</style>
</Unstyled>

export const formFieldTokens = [
  { name: '--ft-form-field-50', value: 'color-mix(primary 2%)', cssValue: 'var(--ft-form-field-50)', usage: 'Subtle hover background on field' },
  { name: '--ft-form-field-100', value: 'color-mix(primary 4%)', cssValue: 'var(--ft-form-field-100)', usage: 'Focused field background tint' },
  { name: '--ft-form-field-200', value: 'color-mix(primary 8%)', cssValue: 'var(--ft-form-field-200)', usage: 'Active / selected field fill' },
  { name: '--ft-form-field-300', value: 'var(--ft-color-n-on-surface-15)', cssValue: 'var(--ft-form-field-300)', usage: 'Border on focused state' },
  { name: '--ft-form-field-muted', value: 'var(--ft-color-n-on-surface-3)', cssValue: 'var(--ft-form-field-muted)', usage: 'Default unfocused field fill' },
  { name: '--ft-form-field-content-muted', value: 'var(--ft-color-n-on-surface-40)', cssValue: 'var(--ft-form-field-content-muted)', usage: 'Placeholder text color' },
  { name: '--ft-form-field-label-muted', value: 'var(--ft-color-n-on-surface-20)', cssValue: 'var(--ft-form-field-label-muted)', usage: 'Floating / inactive label color' },
];

export const switchTokens = [
  { name: '--ft-switch-100', value: 'color-teal-500', cssValue: 'var(--ft-switch-100)', usage: 'Active track (primary ON state)' },
  { name: '--ft-switch-on-100', value: 'n-surface-100', cssValue: 'var(--ft-switch-on-100)', usage: 'Thumb color on primary ON state' },
  { name: '--ft-switch-200', value: 'n-on-surface-30', cssValue: 'var(--ft-switch-200)', usage: 'Inactive track (OFF state)' },
  { name: '--ft-switch-on-200', value: 'n-surface-100', cssValue: 'var(--ft-switch-on-200)', usage: 'Thumb on OFF state' },
  { name: '--ft-switch-muted', value: 'n-on-surface-15', cssValue: 'var(--ft-switch-muted)', usage: 'Disabled track' },
  { name: '--ft-switch-on-muted', value: 'n-on-surface-30', cssValue: 'var(--ft-switch-on-muted)', usage: 'Thumb on disabled state' },
];

export const dialogTokens = [
  { name: '--ft-dialog-v-padding', value: 'var(--ft-unit-400)', px: '16px', axis: 'v', usage: 'Top / bottom padding inside dialogs' },
  { name: '--ft-dialog-h-padding', value: 'var(--ft-unit-800)', px: '32px', axis: 'h', usage: 'Left / right padding inside dialogs' },
];

export const iconTokens = [
  { name: '--ft-icon-100', value: 'n-on-surface-100', cssValue: 'var(--ft-icon-100)', usage: 'Primary icon — full opacity' },
  { name: '--ft-icon-200', value: 'n-on-surface-15', cssValue: 'var(--ft-icon-200)', usage: 'Secondary icon — decorative/subtle' },
  { name: '--ft-icon-muted', value: 'n-on-surface-15', cssValue: 'var(--ft-icon-muted)', usage: 'Disabled / inactive icon' },
];

export const radioTokens = [
  { name: '--ft-radio-muted', value: 'n-on-surface-7', cssValue: 'var(--ft-radio-muted)', usage: 'Unselected radio button border' },
];

export const PlayIcon = ({ color }) => (
  <svg className="ft-icon-svg" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" style={{ color }}>
    <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 14.5v-9l6 4.5-6 4.5z" fill="currentColor" />
  </svg>
);

export const MappedRow = ({ name, value, cssValue, usage }) => (
  <div className="mapped-row">
    {cssValue
      ? <div className="mapped-swatch" style={{ backgroundColor: cssValue }}></div>
      : <div className="mapped-no-swatch">—</div>
    }
    <div className="mapped-info">
      <code className="mapped-varname">{name}</code>
      <span className="mapped-value">{value}</span>
      {usage && <span className="mapped-usage">✦ {usage}</span>}
    </div>
  </div>
);

<Unstyled>
  <div className="mapped-page">

    <div>
      <div className="mapped-section-title">Form Field</div>
      <p className="mapped-section-desc">
        Form field tokens control input backgrounds, borders, and text at each interaction state (default, hover, focused, active, disabled).
      </p>
      <div className="form-demo">
        <span className="form-demo-label" style={{ color: 'var(--ft-form-field-label-muted)' }}>Field Label</span>
        <input className="form-demo-input" placeholder="Placeholder text…" readOnly/>
        <div className="form-demo-states">
          <div className="form-demo-chip" style={{ background: 'var(--ft-form-field-50)', color: 'var(--ft-color-solid-on-surface)', border: '1px solid var(--ft-form-field-300)' }}>Hover (50)</div>
          <div className="form-demo-chip" style={{ background: 'var(--ft-form-field-100)', color: 'var(--ft-color-solid-on-surface)', border: '1px solid var(--ft-form-field-300)' }}>Focus (100)</div>
          <div className="form-demo-chip" style={{ background: 'var(--ft-form-field-200)', color: 'var(--ft-color-solid-on-surface)', border: '1px solid var(--ft-form-field-300)' }}>Active (200)</div>
          <div className="form-demo-chip" style={{ background: 'var(--ft-form-field-muted)', color: 'var(--ft-form-field-content-muted)', border: '1px solid var(--ft-color-n-on-surface-5)' }}>Default (muted)</div>
        </div>
      </div>
      <div className="mapped-grid">
        {formFieldTokens.map(t => <MappedRow key={t.name} name={t.name} value={t.value} cssValue={t.cssValue} usage={t.usage}/>)}
      </div>
    </div>

    <div>
      <div className="mapped-section-title">Switch / Toggle</div>
      <p className="mapped-section-desc">
        Switch tokens define the track and thumb colors for all toggle states. The demo shows ON, OFF, and disabled states.
      </p>
      <div className="switch-demo">
        <div className="switch-item">
          <div className="switch-track" style={{ background: 'var(--ft-switch-100)' }}>
            <div className="switch-thumb" style={{ left: '23px', background: 'var(--ft-switch-on-100)' }}></div>
          </div>
          <span className="switch-label">ON<br/>(switch-100)</span>
        </div>
        <div className="switch-item">
          <div className="switch-track" style={{ background: 'var(--ft-switch-200)' }}>
            <div className="switch-thumb" style={{ left: '3px', background: 'var(--ft-switch-on-200)' }}></div>
          </div>
          <span className="switch-label">OFF<br/>(switch-200)</span>
        </div>
        <div className="switch-item">
          <div className="switch-track" style={{ background: 'var(--ft-switch-muted)', opacity: 0.5 }}>
            <div className="switch-thumb" style={{ left: '3px', background: 'var(--ft-switch-on-muted)' }}></div>
          </div>
          <span className="switch-label">Disabled<br/>(switch-muted)</span>
        </div>
      </div>
      <div className="mapped-grid">
        {switchTokens.map(t => <MappedRow key={t.name} name={t.name} value={t.value} cssValue={t.cssValue} usage={t.usage}/>)}
      </div>
    </div>

    <div>
      <div className="mapped-section-title">Dialog / Modal</div>
      <p className="mapped-section-desc">
        Dialog tokens control internal spacing. The annotated diagram below shows the vertical (16px) and horizontal (32px) padding zones in context.
      </p>
      <div className="mapped-grid" style={{ marginBottom: '16px' }}>
        {dialogTokens.map(t => (
          <div className="mapped-row" key={t.name}>
            <div className="mapped-no-swatch">{t.axis === 'v' ? '↕' : '↔'}</div>
            <div className="mapped-info">
              <code className="mapped-varname">{t.name}</code>
              <span className="mapped-value">{t.value} = {t.px}</span>
              <span className="mapped-usage">✦ {t.usage}</span>
            </div>
            <span className="mapped-badge">spacing</span>
          </div>
        ))}
      </div>
      <div className="dialog-annotation-wrap">
        <div className="dialog-annotation-box">
          <div className="ann-band ann-v" style={{ top: 0, borderBottom: '2px dashed #8b5cf6' }}>
            <span className="ann-label">--ft-dialog-v-padding · 16px</span>
          </div>
          <div className="ann-band ann-v" style={{ bottom: 0, borderTop: '2px dashed #8b5cf6' }}>
            <span className="ann-label">--ft-dialog-v-padding · 16px</span>
          </div>
          <div className="ann-band ann-h" style={{ left: 0, borderRight: '2px dashed #ec4899' }}>
            <span className="ann-label ann-label-h">--ft-dialog-h-padding · 32px</span>
          </div>
          <div className="ann-band ann-h" style={{ right: 0, borderLeft: '2px dashed #ec4899' }}>
            <span className="ann-label ann-label-h">--ft-dialog-h-padding · 32px</span>
          </div>
          <div className="ann-content">
            <div className="ann-title">Dialog Title</div>
            <div className="ann-body">Content goes here inside the dialog body area.</div>
          </div>
        </div>
        <div className="ann-legend">
          <span className="ann-legend-item" style={{ borderColor: '#8b5cf6' }}>↕ Vertical padding (top &amp; bottom)</span>
          <span className="ann-legend-item" style={{ borderColor: '#ec4899' }}>↔ Horizontal padding (left &amp; right)</span>
        </div>
      </div>
    </div>

    <div>
      <div className="mapped-section-title">Icon</div>
      <p className="mapped-section-desc">
        Icon tokens set the fill color for icons across hierarchy levels — primary (full), secondary (decorative), and muted (disabled).
      </p>
      <div className="ft-icon-demo">
        <div className="ft-icon-item">
          <div className="ft-icon-circle">
            <PlayIcon color="var(--ft-icon-100)"/>
          </div>
          <span className="ft-icon-label">icon-100<br/>(primary)</span>
        </div>
        <div className="ft-icon-item">
          <div className="ft-icon-circle">
            <PlayIcon color="var(--ft-icon-200)"/>
          </div>
          <span className="ft-icon-label">icon-200<br/>(secondary)</span>
        </div>
        <div className="ft-icon-item">
          <div className="ft-icon-circle">
            <PlayIcon color="var(--ft-icon-muted)"/>
          </div>
          <span className="ft-icon-label">icon-muted<br/>(disabled)</span>
        </div>
        <div className="ft-icon-variant-list">
          <div style={{ display: 'flex', gap: '20px', alignItems: 'center', flexWrap: 'wrap' }}>
            {[
              { label: 'Search', path: 'M15.5 14h-.79l-.28-.27A6.5 6.5 0 1 0 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z' },
              { label: 'Bell',   path: 'M12 22c1.1 0 2-.9 2-2h-4c0 1.1.9 2 2 2zm6-6v-5c0-3.07-1.63-5.64-4.5-6.32V4c0-.83-.67-1.5-1.5-1.5s-1.5.67-1.5 1.5v.68C7.64 5.36 6 7.92 6 11v5l-2 2v1h16v-1l-2-2z' },
              { label: 'Home',   path: 'M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z' },
            ].map(({ label, path }) => (
              <div key={label} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '6px' }}>
                <svg className="variant-svg" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" style={{ color: 'var(--ft-icon-100)' }}>
                  <path d={path} fill="currentColor" />
                </svg>
                <span style={{ fontSize: '0.65rem', color: 'var(--ft-color-on-surface-muted)', fontFamily: 'monospace' }}>{label}</span>
              </div>
            ))}
          </div>
        </div>
      </div>
      <div className="mapped-grid">
        {iconTokens.map(t => <MappedRow key={t.name} name={t.name} value={t.value} cssValue={t.cssValue} usage={t.usage}/>)}
      </div>
    </div>

    <div>
      <div className="mapped-section-title">Radio</div>
      <p className="mapped-section-desc">
        The radio muted token sets the border color for unselected radio buttons.
      </p>
      <div className="radio-demo">
        <div className="radio-item">
          <div className="radio-circle" style={{ borderColor: 'var(--ft-color-primary)', background: 'var(--ft-color-primary-100)' }}>
            <div className="radio-dot" style={{ background: 'var(--ft-color-primary)' }}></div>
          </div>
          Selected
        </div>
        <div className="radio-item">
          <div className="radio-circle" style={{ borderColor: 'var(--ft-radio-muted)', background: 'transparent' }}></div>
          Unselected <code style={{ fontSize: '0.7rem', color: 'var(--ft-color-on-surface-muted)' }}>(radio-muted)</code>
        </div>
      </div>
      <div className="mapped-grid">
        {radioTokens.map(t => <MappedRow key={t.name} name={t.name} value={t.value} cssValue={t.cssValue} usage={t.usage}/>)}
      </div>
    </div>

  </div>
</Unstyled>
